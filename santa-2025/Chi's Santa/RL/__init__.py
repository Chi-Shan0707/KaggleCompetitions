"""
English:
	This empty file marks the folder as a Python package so editors (e.g., Pylance)
	can resolve relative imports like `from .env_simple import SimplePackingEnv`.
	It also lets you run modules via `python -m <package.module>` when needed.

中文：
	这个空文件用于声明该目录是一个 Python 包，从而让编辑器（如 Pylance）
	正确解析相对导入（例如 `from .env_simple import SimplePackingEnv`）。
	同时也支持通过 `python -m <package.module>` 的方式运行模块。
"""

